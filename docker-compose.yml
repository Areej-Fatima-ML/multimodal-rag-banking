version: '3.8'
services:
  app:
    build: .
    container_name: banking-rag-project
    ports:
      - "8000:8000"
      - "8501:8501"
    volumes:
      - .:/app
    env_file:
      - .env
    restart: unless-stopped